export const enum Settings {
  offers = 500
}

export const enum AppRoute {
  Login = '/login',
  Favorites = '/favorites',
  Offer = '/offer/'
}

export const enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
